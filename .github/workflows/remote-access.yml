name: Provide remote access for debugging
on:
  workflow_dispatch:

jobs:
  setup-arm-builder:
    uses: ./.github/workflows/arm-builder.yml

  start-arm-builder:
    needs: setup-arm-builder

    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/download-artifact@main
      - run: |
          sudo cp -R .ssh /root

      # - uses: wimpysworld/nothing-but-nix@main
      - uses: actions/download-artifact@main
        with:
          name: nix-install-${{ runner.arch }}
          path: /nix

      - uses: cachix/install-nix-action@master
        with:
          install_url: file:///nix/install

      - run: sudo ssh proxy-jump -N

  setup-remote-access:
    needs: setup-arm-builder

    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@main
      - run: |
          mv nix ~/.config
          chmod 600 .ssh/id_rsa
          sudo cp -R .ssh /root

      # - uses: wimpysworld/nothing-but-nix@main
      - uses: actions/download-artifact@main
        with:
          name: nix-install-${{ runner.arch }}
          path: /nix

      - uses: cachix/install-nix-action@master
        with:
          install_url: file:///nix/install

      - uses: cachix/cachix-action@master
        with:
          name: m-bdf
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - uses: actions/checkout@main
      - uses: owenthereal/action-upterm@main

      - if: always()
        run: sudo ssh arm-builder -f killall ssh
